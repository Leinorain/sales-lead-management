<!DOCTYPE html>
<html>
<head>
    <title>Leads</title>
</head>
<body>
    <h1>Lead List</h1>
    <ul>
        {% for lead in leads %}
            <li>{{ lead.getName() }} {{ lead.getEmail() }} </li>
        {% else %}
            <li>No leads found.</li>
        {% endfor %}
    </ul>

    <h2>Create Lead</h2>
    <form method="post" action="{{ url_for('lead.create') }}">
        <input type="text" name="name" placeholder="Name" required>
        <input type="text" name="contactNumber" placeholder="Contact Number" required>
        <input type="email" name="email" placeholder="Email" required>
        <input type="text" name="productInterest" placeholder="Product Interest" required>
        <button type="submit">Create Lead</button>
    </form>
</body>
</html>
